import { ReactElement } from "react";

import StyledSpan from "../../elements/styled-span.component";

function FractionDelimeter({ ...props }): ReactElement {
  return <StyledSpan {...props} />;
}

export default FractionDelimeter;
