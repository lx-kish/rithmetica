export const routes = {
  root: "/",
  default: "/arithmetic",
  multiplicationTab: "/multiplication-tab",
  arithmetic: "/arithmetic",
  fractions: "/fractions",
} as const;

export const numberOfColumns = {
  one: 1,
  two: 2,
} as const;
